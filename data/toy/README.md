# Toy smoke data

Run `python scripts/create_toy_dataset.py --output data/toy` to regenerate deterministic project-owned images and annotations. They are for I/O, metric and codec smoke tests only. They are prohibited from scientific performance claims, model selection and final qualitative figures.
