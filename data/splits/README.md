# Dataset splits

Store split manifests or sample-ID lists here. Never create a split by moving raw files. Each split must record source manifest hash, generation command/seed, duplicate audit and intended role. Test IDs must not be used for training, utility-label generation, early stopping or threshold selection.
