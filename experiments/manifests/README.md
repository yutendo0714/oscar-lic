# Experiment Manifests

This directory is populated by registered runs. Every artifact must be traceable to an experiment ID, run manifest, config hash, code commit, environment hash, dataset manifest and seed. Do not place unregistered ad-hoc outputs here.
