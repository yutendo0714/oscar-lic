name: oscar-mlic-repro
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pytorch=2.0.1
  - torchvision=0.15.2
  - pytorch-cuda=11.8
  - pip
  - pip:
      - compressai==1.2.0b3
      - einops>=0.6,<0.8
      - timm>=0.9,<1.0
      - tensorboard>=2.12,<3
      - pytorch-msssim>=1.0
      - pillow>=9.5,<11
      - numpy>=1.23,<2
      - scipy>=1.9,<2
      - tqdm>=4.64
# Candidate faithful environment derived from MLIC README.
# It is not marked validated until built on target hardware and locked.
