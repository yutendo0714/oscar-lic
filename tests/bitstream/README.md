# Bitstream conformance tests

Store golden `.oscr` fixtures only when they contain project-owned synthetic content. Test header parsing, unknown mandatory sections, CRC, truncation, overlap, byte accounting, base-only decode and version compatibility.
