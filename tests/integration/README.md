# Integration tests

Integration tests should cover real serialized streams, adapter boundaries and fixed small model bundles. Mark network/GPU/external-weight tests explicitly; default CI must not download large or license-restricted assets.
