# Reproducibility tests

These tests validate manifests, configuration hashes, deterministic toy generation and release integrity. Hardware-specific numerical tolerances must be explicit and must not be silently widened.
